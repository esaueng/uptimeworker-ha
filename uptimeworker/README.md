# Uptime Worker HA

Uptime Worker HA runs the retained Node.js monitoring server as a Home
Assistant add-on. It serves the web UI on port `3001` and stores its SQLite
database and runtime data in the add-on `/data` directory managed by Home
Assistant.

Open the web UI from the add-on page after installation, then create the first
admin user from the setup screen.
