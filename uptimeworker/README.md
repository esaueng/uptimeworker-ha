# Uptime Worker

Uptime Worker runs the retained Node.js monitoring server as a Home Assistant
app. It serves the web UI on port `3001` and stores its SQLite database and
runtime data in the app `/data` directory managed by Home Assistant.

Open the web UI from the app page after installation, then create the first
admin user from the setup screen.
