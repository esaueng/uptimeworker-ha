services:
  uptimeworker:
    build:
      context: .
      dockerfile: Dockerfile
    image: uptimeworker-ha:local
    container_name: uptimeworker-ha
    restart: unless-stopped
    volumes:
      - ./data:/data
    ports:
      # <Host Port>:<Container Port>
      - "3001:3001"
